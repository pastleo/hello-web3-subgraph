{
  "name": "hello-web3-subgraph",
  "license": "UNLICENSED",
  "scripts": {
    "codegen": "graph codegen",
    "build": "graph build",
    "deploy": "graph deploy --node https://api.studio.thegraph.com/deploy/ hello-web3-subgraph",
    "create-local": "graph create --node http://localhost:8020/ hello-web3-subgraph",
    "remove-local": "graph remove --node http://localhost:8020/ hello-web3-subgraph",
    "deploy-local": "graph deploy --node http://localhost:8020/ --ipfs http://localhost:5001 hello-web3-subgraph"
  },
  "dependencies": {
    "@graphprotocol/graph-cli": "0.29.2",
    "@graphprotocol/graph-ts": "0.26.0"
  }
}
